From b9e9986d6cfa84d2619b866dbe3d70272c6ffdd7 Mon Sep 17 00:00:00 2001
From: Sais <github@swgsais.com>
Date: Sat, 18 Jul 2026 15:44:37 -0400
Subject: [PATCH] Author Publish 14 incapacitation recovery lifecycle

---
 .../compiled/game/script/library/pclib.java   | 129 +++-
 .../game/script/player/base/base_player.java  | 147 ++--
 ...precu_incapacitation_recovery_fixture.java | 644 ++++++++++++++++++
 3 files changed, 863 insertions(+), 57 deletions(-)
 create mode 100644 sku.0/sys.server/compiled/game/script/test/precu_incapacitation_recovery_fixture.java

diff --git a/sku.0/sys.server/compiled/game/script/library/pclib.java b/sku.0/sys.server/compiled/game/script/library/pclib.java
index 6582703d0..ce6dbc98e 100755
--- a/sku.0/sys.server/compiled/game/script/library/pclib.java
+++ b/sku.0/sys.server/compiled/game/script/library/pclib.java
@@ -104,9 +104,13 @@ public class pclib extends script.base_script
     public static final String VAR_CORPSE_ID = "corpse.id";
     public static final String VAR_CORPSE_KILLER = "corpse.killer";
     public static final String VAR_CORPSE_STAMP = "corpse.stamp";
-    public static final String VAR_BEEN_COUPDEGRACED = "beenCoupDeGraced";
-    public static final String VAR_DEATHBLOW_KILLER = VAR_BEEN_COUPDEGRACED + ".killer";
-    public static final String VAR_DEATHBLOW_STAMP = VAR_BEEN_COUPDEGRACED + ".stamp";
+    public static final String VAR_BEEN_COUPDEGRACED = "beenCoupDeGraced";
+    public static final String VAR_DEATHBLOW_KILLER = VAR_BEEN_COUPDEGRACED + ".killer";
+    public static final String VAR_DEATHBLOW_STAMP = VAR_BEEN_COUPDEGRACED + ".stamp";
+    public static final String VAR_PRECU_INCAPACITATION_TIMES =
+        "combat.precuIncapacitationTimes";
+    public static final int PRECU_INCAPACITATION_WINDOW = 600;
+    public static final int PRECU_INCAPACITATION_LIMIT = 3;
     public static final String DATATABLE_AI_SPECIES = "datatables/ai/species.iff";
     public static final String DATATABLE_COL_SKELETON = "Skeleton";
     public static final String SKELETON_HUMAN = "human";
@@ -545,7 +549,7 @@ public class pclib extends script.base_script
         }
         return false;
     }
-    public static location getEffectiveDeathLocation(obj_id player) throws InterruptedException
+    public static location getEffectiveDeathLocation(obj_id player) throws InterruptedException
     {
         location loc = getLocation(player);
         if (!loc.area.startsWith("space_"))
@@ -557,9 +561,114 @@ public class pclib extends script.base_script
         {
             return loc;
         }
-        return new location(0.0f, 0.0f, 0.0f, "tatooine");
-    }
-    public static void killPlayer(obj_id player, obj_id killer) throws InterruptedException
+        return new location(0.0f, 0.0f, 0.0f, "tatooine");
+    }
+    public static int calculatePrecuIncapacitationTimer(int condition)
+        throws InterruptedException
+    {
+        int value = -condition;
+        if (value < 5)
+        {
+            return 5;
+        }
+        int recoveryTime = value / 5;
+        return recoveryTime > 60 ? 60 : recoveryTime;
+    }
+    public static int getPrecuIncapacitationCount(obj_id player)
+        throws InterruptedException
+    {
+        if (!isIdValid(player) ||
+            !hasObjVar(player, VAR_PRECU_INCAPACITATION_TIMES))
+        {
+            return 0;
+        }
+        int[] timestamps =
+            getIntArrayObjVar(
+                player,
+                VAR_PRECU_INCAPACITATION_TIMES);
+        if (timestamps == null || timestamps.length == 0)
+        {
+            return 0;
+        }
+        int now = getGameTime();
+        int oldestAllowed =
+            now - PRECU_INCAPACITATION_WINDOW;
+        int count = 0;
+        for (int timestamp : timestamps)
+        {
+            if (timestamp > oldestAllowed &&
+                timestamp <= now)
+            {
+                ++count;
+            }
+        }
+        return count;
+    }
+    public static int addPrecuIncapacitationTime(obj_id player)
+        throws InterruptedException
+    {
+        if (!isIdValid(player))
+        {
+            return 0;
+        }
+        int[] existing =
+            hasObjVar(
+                player,
+                VAR_PRECU_INCAPACITATION_TIMES)
+                ? getIntArrayObjVar(
+                    player,
+                    VAR_PRECU_INCAPACITATION_TIMES)
+                : null;
+        int now = getGameTime();
+        int oldestAllowed =
+            now - PRECU_INCAPACITATION_WINDOW;
+        int active = 0;
+        if (existing != null)
+        {
+            for (int timestamp : existing)
+            {
+                if (timestamp > oldestAllowed &&
+                    timestamp <= now)
+                {
+                    ++active;
+                }
+            }
+        }
+        int[] updated = new int[active + 1];
+        int index = 0;
+        if (existing != null)
+        {
+            for (int timestamp : existing)
+            {
+                if (timestamp > oldestAllowed &&
+                    timestamp <= now)
+                {
+                    updated[index++] = timestamp;
+                }
+            }
+        }
+        updated[index] = now;
+        setObjVar(
+            player,
+            VAR_PRECU_INCAPACITATION_TIMES,
+            updated);
+        return updated.length;
+    }
+    public static void clearPrecuIncapacitationTimes(obj_id player)
+        throws InterruptedException
+    {
+        if (!isIdValid(player))
+        {
+            return;
+        }
+        removeObjVar(
+            player,
+            VAR_PRECU_INCAPACITATION_TIMES);
+        removeObjVar(
+            player,
+            "combat.intIncapacitationCount");
+    }
+    public static void killPlayer(obj_id player, obj_id killer) throws InterruptedException
     {
         killPlayer(player, killer, true);
     }
@@ -799,8 +908,10 @@ public class pclib extends script.base_script
                 guildUpdateGuildWarKillTracking(killer, player);
             }
         }
-        factions.grantCombatFaction(killer, player, factionMod);
-        dot.removeAllDots(player);
+        factions.grantCombatFaction(killer, player, factionMod);
+        clearPrecuIncapacitationTimes(player);
+        utils.removeScriptVar(player, "incap.timeStamp");
+        dot.removeAllDots(player);
         clearAllHate(player);
         playMusic(player, "sound/music_player_death.snd");
         obj_id gid = getGroupObject(player);
diff --git a/sku.0/sys.server/compiled/game/script/player/base/base_player.java b/sku.0/sys.server/compiled/game/script/player/base/base_player.java
index e983b172a..82fd25363 100755
--- a/sku.0/sys.server/compiled/game/script/player/base/base_player.java
+++ b/sku.0/sys.server/compiled/game/script/player/base/base_player.java
@@ -990,9 +990,13 @@ public class base_player extends script.base_script
         }
         return SCRIPT_CONTINUE;
     }
-    public int OnIncapacitated(obj_id self, obj_id killer) throws InterruptedException
-    {
-        utils.setScriptVar(self, "lastKiller", killer);
+    public int OnIncapacitated(obj_id self, obj_id killer) throws InterruptedException
+    {
+        utils.setScriptVar(self, "lastKiller", killer);
+        if (!isIdValid(killer))
+        {
+            killer = self;
+        }
         if (hasScript(killer, "ai.smuggler_spawn_enemy"))
         {
             int exceptionSmuggler = 0;
@@ -1023,31 +1027,30 @@ public class base_player extends script.base_script
         {
             buff.removeBuff(self, vehicleBuff);
         }
-        if (buff.hasBuff(self, "incapWeaken"))
-        {
-            pclib.killPlayer(self, killer, true);
-            return SCRIPT_CONTINUE;
-        }
-        else 
-        {
-            buff.applyBuff(self, "incapWeaken");
-        }
-        int recapacitateTimer = 10;
-        float recapacitateModified = getEnhancedSkillStatisticModifierUncapped(self, "resistance_incapacitation");
-        if (recapacitateModified > 0)
-        {
-            recapacitateTimer -= recapacitateModified;
-            if (recapacitateTimer <= 2)
-            {
-                recapacitateTimer = 2;
-            }
-            sendSystemMessage(self, SID_INCAPACITATION_DURATION_REDUCED);
-        }
-        CustomerServiceLog("player_death", "Player %TU is incapacitated by %TT", self, killer);
-        if (!isIdValid(killer))
-        {
-            killer = self;
-        }
+        if (buff.hasBuff(self, "incapWeaken"))
+        {
+            buff.removeBuff(self, "incapWeaken");
+        }
+        removeObjVar(self, "combat.intIncapacitationCount");
+        int incapacitationCount =
+            pclib.addPrecuIncapacitationTime(self);
+        if (incapacitationCount >=
+            pclib.PRECU_INCAPACITATION_LIMIT)
+        {
+            pclib.clearPrecuIncapacitationTimes(self);
+            utils.removeScriptVar(self, "incap.timeStamp");
+            pclib.killPlayer(self, killer, true);
+            return SCRIPT_CONTINUE;
+        }
+        int condition = getAttrib(self, HEALTH);
+        condition =
+            Math.min(condition, getAttrib(self, ACTION));
+        condition =
+            Math.min(condition, getAttrib(self, MIND));
+        int recapacitateTimer =
+            pclib.calculatePrecuIncapacitationTimer(
+                condition);
+        CustomerServiceLog("player_death", "Player %TU is incapacitated by %TT", self, killer);
         if (killer == self)
         {
             sendSystemMessage(self, SID_VICTIM_INCAPACITATED);
@@ -1062,12 +1065,26 @@ public class base_player extends script.base_script
             prose_package ppToKiller = prose.getPackage(PROSE_TARGET_INCAP, self);
             sendSystemMessageProse(killer, ppToKiller);
         }
-        utils.removeScriptVar(self, COUPE_DE_GRACE_TARGET);
-        utils.setScriptVar(self, "incap.timeStamp", (getGameTime() + recapacitateTimer));
-        setCount(self, recapacitateTimer);
-        setHealth(self, 0);
-        combat.doCombatDebuffs(self);
-        messageTo(self, "recapacitationDelay", null, recapacitateTimer, false);
+        utils.removeScriptVar(self, COUPE_DE_GRACE_TARGET);
+        int recoveryTime =
+            getGameTime() + recapacitateTimer;
+        utils.setScriptVar(
+            self,
+            "incap.timeStamp",
+            recoveryTime);
+        setCount(self, recapacitateTimer);
+        combat.doCombatDebuffs(self);
+        dictionary recoveryParams =
+            new dictionary();
+        recoveryParams.put(
+            "recoveryTime",
+            recoveryTime);
+        messageTo(
+            self,
+            "recapacitationDelay",
+            recoveryParams,
+            recapacitateTimer,
+            false);
         clearAllAiEnemyFlags(self);
         dot.removeAllDots(self);
         if (stealth.hasInvisibleBuff(self))
@@ -1090,20 +1107,54 @@ public class base_player extends script.base_script
             }
         }
     }
-    public int recapacitationDelay(obj_id self, dictionary params) throws InterruptedException
-    {
-        if (getPosture(self) != POSTURE_INCAPACITATED)
-        {
-            return SCRIPT_CONTINUE;
-        }
-        if (!combat.clearCombatDebuffs(self))
-        {
-            messageTo(self, "recapacitationDelay", null, 1, false);
-            return SCRIPT_CONTINUE;
-        }
-        setHealth(self, 1);
-        return SCRIPT_CONTINUE;
-    }
+    public int recapacitationDelay(obj_id self, dictionary params) throws InterruptedException
+    {
+        if (getPosture(self) != POSTURE_INCAPACITATED)
+        {
+            return SCRIPT_CONTINUE;
+        }
+        if (params == null ||
+            !params.containsKey("recoveryTime") ||
+            !utils.hasScriptVar(self, "incap.timeStamp") ||
+            params.getInt("recoveryTime") !=
+                utils.getIntScriptVar(
+                    self,
+                    "incap.timeStamp"))
+        {
+            return SCRIPT_CONTINUE;
+        }
+        if (!combat.clearCombatDebuffs(self))
+        {
+            messageTo(
+                self,
+                "recapacitationDelay",
+                params,
+                1,
+                false);
+            return SCRIPT_CONTINUE;
+        }
+        setState(
+            self,
+            STATE_FEIGN_DEATH,
+            false);
+        if (getAttrib(self, HEALTH) <= 0)
+        {
+            setAttrib(self, HEALTH, 1);
+        }
+        if (getAttrib(self, ACTION) <= 0)
+        {
+            setAttrib(self, ACTION, 1);
+        }
+        if (getAttrib(self, MIND) <= 0)
+        {
+            setAttrib(self, MIND, 1);
+        }
+        utils.removeScriptVar(
+            self,
+            "incap.timeStamp");
+        setCount(self, 0);
+        return SCRIPT_CONTINUE;
+    }
     public int requestMissionLocations(obj_id self, dictionary params) throws InterruptedException
     {
         obj_id groupObject = getGroupObject(self);
diff --git a/sku.0/sys.server/compiled/game/script/test/precu_incapacitation_recovery_fixture.java b/sku.0/sys.server/compiled/game/script/test/precu_incapacitation_recovery_fixture.java
new file mode 100644
index 000000000..4af8ab781
--- /dev/null
+++ b/sku.0/sys.server/compiled/game/script/test/precu_incapacitation_recovery_fixture.java
@@ -0,0 +1,644 @@
+package script.test;
+
+import script.obj_id;
+import script.library.buff;
+import script.library.pclib;
+import script.library.utils;
+
+/**
+ * ServerConsole-only fixture for the Publish 14.1 incapacitation lifecycle.
+ *
+ * The fixture snapshots one disposable player, disables primary regeneration,
+ * and drives three real attribute transitions through the production
+ * OnIncapacitated handler. It never attaches to a gameplay object.
+ */
+public class precu_incapacitation_recovery_fixture
+    extends script.base_script
+{
+    private static final long PLAYER_OID = 44003778L;
+    private static final int PLAYER_STATION_ID = 91001;
+    private static final String ROOT =
+        "precu.incapacitationRecoveryFixture";
+    private static final String LIFECYCLE =
+        ROOT + ".lifecycle";
+    private static final String PREPARED =
+        ROOT + ".prepared";
+    private static final String ORIGINAL_POSTURE =
+        ROOT + ".originalPosture";
+    private static final String ORIGINAL_LOCOMOTION =
+        ROOT + ".originalLocomotion";
+    private static final String ORIGINAL_ATTRIBUTES =
+        ROOT + ".originalAttributes";
+    private static final String ORIGINAL_REGEN =
+        ROOT + ".originalRegen";
+    private static final String ORIGINAL_TIMES_PRESENT =
+        ROOT + ".originalTimesPresent";
+    private static final String ORIGINAL_TIMES =
+        ROOT + ".originalTimes";
+    private static final int[] ATTRIBUTES =
+    {
+        HEALTH,
+        STRENGTH,
+        CONSTITUTION,
+        ACTION,
+        QUICKNESS,
+        STAMINA,
+        MIND,
+        FOCUS,
+        WILLPOWER
+    };
+    private static final int[] PRIMARY =
+    {
+        HEALTH,
+        ACTION,
+        MIND
+    };
+    private static final String USAGE =
+        "usage: inspect|prepare|timerProbe|incapOne|" +
+        "incapTwo|incapThree|status|cleanup " +
+        "44003778 <32-hex-lifecycle>";
+
+    public String executeFixture(String params)
+        throws InterruptedException
+    {
+        String[] args =
+            params == null
+                ? new String[0]
+                : params.trim().split("[ ]+");
+        if (args.length != 3 ||
+            !args[2].matches("[a-f0-9]{32}"))
+        {
+            return USAGE;
+        }
+
+        long playerValue;
+        try
+        {
+            playerValue = Long.parseLong(args[1]);
+        }
+        catch (NumberFormatException exception)
+        {
+            return "error=invalidOid";
+        }
+        if (playerValue != PLAYER_OID)
+        {
+            return "error=identityNotAllowed";
+        }
+
+        obj_id player = obj_id.getObjId(playerValue);
+        String validation = validatePlayer(player);
+        if (validation != null)
+        {
+            return validation;
+        }
+
+        String action = args[0];
+        String lifecycle = args[2];
+        if (action.equalsIgnoreCase("inspect"))
+        {
+            return "action=inspect " +
+                buildStatus(player, lifecycle);
+        }
+        if (action.equalsIgnoreCase("prepare"))
+        {
+            return prepare(player, lifecycle);
+        }
+        if (action.equalsIgnoreCase("timerProbe"))
+        {
+            return timerProbe(player, lifecycle);
+        }
+        if (action.equalsIgnoreCase("incapOne"))
+        {
+            return incapacitate(
+                player,
+                lifecycle,
+                0,
+                HEALTH,
+                -25,
+                5,
+                false);
+        }
+        if (action.equalsIgnoreCase("incapTwo"))
+        {
+            return incapacitate(
+                player,
+                lifecycle,
+                1,
+                ACTION,
+                -100,
+                20,
+                false);
+        }
+        if (action.equalsIgnoreCase("incapThree"))
+        {
+            return incapacitate(
+                player,
+                lifecycle,
+                2,
+                MIND,
+                -25,
+                0,
+                true);
+        }
+        if (action.equalsIgnoreCase("status"))
+        {
+            validation =
+                validateOwnership(
+                    player,
+                    lifecycle,
+                    false);
+            return validation == null
+                ? "action=status " +
+                    buildStatus(player, lifecycle)
+                : validation;
+        }
+        if (action.equalsIgnoreCase("cleanup"))
+        {
+            return cleanup(player, lifecycle);
+        }
+        return USAGE;
+    }
+
+    private String prepare(
+        obj_id player,
+        String lifecycle)
+        throws InterruptedException
+    {
+        String ownership =
+            validateOwnership(
+                player,
+                lifecycle,
+                true);
+        if (ownership == null)
+        {
+            return getIntObjVar(player, PREPARED) == 1
+                ? "action=prepare resumed=true " +
+                    buildStatus(player, lifecycle)
+                : "error=fixturePartial";
+        }
+        if (!"fixtureAbsent".equals(ownership))
+        {
+            return ownership;
+        }
+        if (isDead(player) ||
+            isIncapacitated(player) ||
+            getPosture(player) != POSTURE_UPRIGHT)
+        {
+            return "error=playerNotUpright";
+        }
+        if (utils.hasScriptVar(player, "incap.timeStamp"))
+        {
+            return "error=incapRecoveryAlreadyPending";
+        }
+        if (buff.hasBuff(player, "incapWeaken"))
+        {
+            return "error=legacyIncapWeakenPresent";
+        }
+        if (hasObjVar(
+                player,
+                "combat.intIncapacitationCount"))
+        {
+            return "error=legacyIncapacitationCountPresent";
+        }
+        for (int attribute : PRIMARY)
+        {
+            if (getAttrib(player, attribute) <= 0)
+            {
+                return "error=primaryPoolNotPositive";
+            }
+        }
+
+        int[] values = new int[ATTRIBUTES.length];
+        for (int index = 0;
+            index < ATTRIBUTES.length;
+            ++index)
+        {
+            values[index] =
+                getAttrib(
+                    player,
+                    ATTRIBUTES[index]);
+        }
+        float[] regen = new float[PRIMARY.length];
+        for (int index = 0;
+            index < PRIMARY.length;
+            ++index)
+        {
+            regen[index] =
+                getRegenRate(
+                    player,
+                    PRIMARY[index]);
+        }
+
+        setObjVar(player, LIFECYCLE, lifecycle);
+        setObjVar(
+            player,
+            ORIGINAL_POSTURE,
+            getPosture(player));
+        setObjVar(
+            player,
+            ORIGINAL_LOCOMOTION,
+            getLocomotion(player));
+        setObjVar(
+            player,
+            ORIGINAL_ATTRIBUTES,
+            values);
+        setObjVar(
+            player,
+            ORIGINAL_REGEN,
+            regen);
+        boolean originalTimesPresent =
+            hasObjVar(
+                player,
+                pclib.VAR_PRECU_INCAPACITATION_TIMES);
+        setObjVar(
+            player,
+            ORIGINAL_TIMES_PRESENT,
+            originalTimesPresent ? 1 : 0);
+        if (originalTimesPresent)
+        {
+            int[] originalTimes =
+                getIntArrayObjVar(
+                    player,
+                    pclib.VAR_PRECU_INCAPACITATION_TIMES);
+            setObjVar(
+                player,
+                ORIGINAL_TIMES,
+                originalTimes == null
+                    ? new int[0]
+                    : originalTimes);
+        }
+
+        stopCombat(player);
+        setCombatTarget(player, obj_id.NULL_ID);
+        for (int attribute : PRIMARY)
+        {
+            setRegenRate(
+                player,
+                attribute,
+                0.0f);
+        }
+        pclib.clearPrecuIncapacitationTimes(player);
+        setObjVar(player, PREPARED, 1);
+
+        return "action=prepare resumed=false " +
+            buildStatus(player, lifecycle);
+    }
+
+    private String timerProbe(
+        obj_id player,
+        String lifecycle)
+        throws InterruptedException
+    {
+        String validation =
+            validateOwnership(
+                player,
+                lifecycle,
+                false);
+        if (validation != null)
+        {
+            return validation;
+        }
+        return "action=timerProbe timer0=" +
+            pclib.calculatePrecuIncapacitationTimer(0) +
+            " timerMinus4=" +
+            pclib.calculatePrecuIncapacitationTimer(-4) +
+            " timerMinus5=" +
+            pclib.calculatePrecuIncapacitationTimer(-5) +
+            " timerMinus25=" +
+            pclib.calculatePrecuIncapacitationTimer(-25) +
+            " timerMinus100=" +
+            pclib.calculatePrecuIncapacitationTimer(-100) +
+            " timerMinus500=" +
+            pclib.calculatePrecuIncapacitationTimer(-500) +
+            " lifecycle=" + lifecycle;
+    }
+
+    private String incapacitate(
+        obj_id player,
+        String lifecycle,
+        int requiredCount,
+        int attribute,
+        int condition,
+        int expectedTimer,
+        boolean expectDeath)
+        throws InterruptedException
+    {
+        String validation =
+            validateOwnership(
+                player,
+                lifecycle,
+                false);
+        if (validation != null)
+        {
+            return validation;
+        }
+        if (getIntObjVar(player, PREPARED) != 1)
+        {
+            return "error=fixtureNotPrepared";
+        }
+        if (isDead(player) ||
+            isIncapacitated(player) ||
+            getPosture(player) != POSTURE_UPRIGHT)
+        {
+            return "error=playerNotRecovered " +
+                buildStatus(player, lifecycle);
+        }
+        int beforeCount =
+            pclib.getPrecuIncapacitationCount(player);
+        if (beforeCount != requiredCount)
+        {
+            return "error=unexpectedCounter expected=" +
+                requiredCount + " actual=" +
+                beforeCount;
+        }
+
+        int beforeTime = getGameTime();
+        setAttrib(player, attribute, condition);
+        int afterTime = getGameTime();
+        int afterCount =
+            pclib.getPrecuIncapacitationCount(player);
+        boolean dead = isDead(player);
+        int recoveryTime =
+            utils.hasScriptVar(player, "incap.timeStamp")
+                ? utils.getIntScriptVar(
+                    player,
+                    "incap.timeStamp")
+                : 0;
+        boolean timerMatched =
+            expectDeath
+                ? recoveryTime == 0
+                : recoveryTime >=
+                    beforeTime + expectedTimer &&
+                    recoveryTime <=
+                        afterTime + expectedTimer;
+        boolean transitionMatched =
+            expectDeath
+                ? dead && afterCount == 0
+                : !dead &&
+                    getPosture(player) ==
+                        POSTURE_INCAPACITATED &&
+                    afterCount == requiredCount + 1 &&
+                    timerMatched;
+        return (transitionMatched
+                ? "action=incapacitate result=passed"
+                : "error=incapacitationTransitionFailed") +
+            " requiredCount=" + requiredCount +
+            " afterCount=" + afterCount +
+            " attribute=" + attribute +
+            " condition=" + condition +
+            " expectedTimer=" + expectedTimer +
+            " recoveryTime=" + recoveryTime +
+            " timerMatched=" + timerMatched +
+            " expectDeath=" + expectDeath +
+            " " + buildStatus(player, lifecycle);
+    }
+
+    private String cleanup(
+        obj_id player,
+        String lifecycle)
+        throws InterruptedException
+    {
+        String ownership =
+            validateOwnership(
+                player,
+                lifecycle,
+                true);
+        if ("fixtureAbsent".equals(ownership))
+        {
+            return "action=cleanup alreadyClean=true" +
+                " restored=true lifecycle=" +
+                lifecycle;
+        }
+        if (ownership != null)
+        {
+            return ownership;
+        }
+        if (!hasCompleteSnapshot(player))
+        {
+            return "error=incompleteSnapshot";
+        }
+
+        utils.removeScriptVar(player, "incap.timeStamp");
+        pclib.clearPrecuIncapacitationTimes(player);
+        if (isDead(player))
+        {
+            pclib.resurrectPlayer(player);
+        }
+        setState(player, STATE_FEIGN_DEATH, false);
+        boolean postureRestored =
+            setPostureClientImmediate(
+                player,
+                getIntObjVar(
+                    player,
+                    ORIGINAL_POSTURE));
+        boolean locomotionRestored =
+            setLocomotion(
+                player,
+                getIntObjVar(
+                    player,
+                    ORIGINAL_LOCOMOTION));
+
+        int[] values =
+            getIntArrayObjVar(
+                player,
+                ORIGINAL_ATTRIBUTES);
+        boolean attributesRestored =
+            values != null &&
+                values.length == ATTRIBUTES.length;
+        if (attributesRestored)
+        {
+            for (int index = 0;
+                index < ATTRIBUTES.length;
+                ++index)
+            {
+                setAttrib(
+                    player,
+                    ATTRIBUTES[index],
+                    values[index]);
+                attributesRestored =
+                    getAttrib(
+                        player,
+                        ATTRIBUTES[index]) ==
+                        values[index] &&
+                    attributesRestored;
+            }
+        }
+
+        float[] regen =
+            getFloatArrayObjVar(
+                player,
+                ORIGINAL_REGEN);
+        boolean regenRestored =
+            regen != null &&
+                regen.length == PRIMARY.length;
+        if (regenRestored)
+        {
+            for (int index = 0;
+                index < PRIMARY.length;
+                ++index)
+            {
+                setRegenRate(
+                    player,
+                    PRIMARY[index],
+                    regen[index]);
+                regenRestored =
+                    getRegenRate(
+                        player,
+                        PRIMARY[index]) ==
+                        regen[index] &&
+                    regenRestored;
+            }
+        }
+
+        if (getIntObjVar(
+                player,
+                ORIGINAL_TIMES_PRESENT) == 1)
+        {
+            setObjVar(
+                player,
+                pclib.VAR_PRECU_INCAPACITATION_TIMES,
+                getIntArrayObjVar(
+                    player,
+                    ORIGINAL_TIMES));
+        }
+        else
+        {
+            pclib.clearPrecuIncapacitationTimes(player);
+        }
+        removeObjVar(
+            player,
+            pclib.VAR_BEEN_COUPDEGRACED);
+
+        boolean restored =
+            postureRestored &&
+            locomotionRestored &&
+            attributesRestored &&
+            regenRestored &&
+            !isDead(player) &&
+            !isIncapacitated(player);
+        if (!restored)
+        {
+            return "error=fixtureRestoreFailed " +
+                buildStatus(player, lifecycle);
+        }
+        removeObjVar(player, ROOT);
+        return "action=cleanup alreadyClean=false" +
+            " restored=" + !hasObjVar(player, ROOT) +
+            " lifecycle=" + lifecycle;
+    }
+
+    private String validatePlayer(obj_id player)
+        throws InterruptedException
+    {
+        if (player == null ||
+            player == obj_id.NULL_ID ||
+            !player.isLoaded())
+        {
+            return "error=playerNotLoaded";
+        }
+        if (!player.isAuthoritative() ||
+            !isPlayer(player))
+        {
+            return "error=playerNotAuthoritative";
+        }
+        if (getPlayerStationId(player) !=
+            PLAYER_STATION_ID)
+        {
+            return "error=stationNotAllowed";
+        }
+        return null;
+    }
+
+    private String validateOwnership(
+        obj_id player,
+        String lifecycle,
+        boolean allowAbsent)
+        throws InterruptedException
+    {
+        if (!hasObjVar(player, ROOT))
+        {
+            return allowAbsent
+                ? "fixtureAbsent"
+                : "error=fixtureAbsent";
+        }
+        if (!hasObjVar(player, LIFECYCLE))
+        {
+            return "error=fixturePartial";
+        }
+        if (!lifecycle.equals(
+                getStringObjVar(
+                    player,
+                    LIFECYCLE)))
+        {
+            return "error=fixtureOwnershipMismatch";
+        }
+        return null;
+    }
+
+    private boolean hasCompleteSnapshot(obj_id player)
+        throws InterruptedException
+    {
+        if (!hasObjVar(player, LIFECYCLE) ||
+            !hasObjVar(player, PREPARED) ||
+            !hasObjVar(player, ORIGINAL_POSTURE) ||
+            !hasObjVar(player, ORIGINAL_LOCOMOTION) ||
+            !hasObjVar(player, ORIGINAL_ATTRIBUTES) ||
+            !hasObjVar(player, ORIGINAL_REGEN) ||
+            !hasObjVar(
+                player,
+                ORIGINAL_TIMES_PRESENT))
+        {
+            return false;
+        }
+        return getIntObjVar(
+                player,
+                ORIGINAL_TIMES_PRESENT) != 1 ||
+            hasObjVar(player, ORIGINAL_TIMES);
+    }
+
+    private String buildStatus(
+        obj_id player,
+        String lifecycle)
+        throws InterruptedException
+    {
+        int recoveryTime =
+            utils.hasScriptVar(player, "incap.timeStamp")
+                ? utils.getIntScriptVar(
+                    player,
+                    "incap.timeStamp")
+                : 0;
+        int remaining =
+            recoveryTime > 0
+                ? Math.max(
+                    0,
+                    recoveryTime - getGameTime())
+                : 0;
+        return "lifecycle=" + lifecycle +
+            " fixtureLifecycle=" +
+                (hasObjVar(player, LIFECYCLE)
+                    ? getStringObjVar(
+                        player,
+                        LIFECYCLE)
+                    : "none") +
+            " prepared=" +
+                (getIntObjVar(player, PREPARED) == 1) +
+            " posture=" + getPosture(player) +
+            " locomotion=" + getLocomotion(player) +
+            " dead=" + isDead(player) +
+            " incapacitated=" +
+                isIncapacitated(player) +
+            " health=" +
+                getAttrib(player, HEALTH) +
+            " action=" +
+                getAttrib(player, ACTION) +
+            " mind=" +
+                getAttrib(player, MIND) +
+            " activeCount=" +
+                pclib.getPrecuIncapacitationCount(
+                    player) +
+            " recoveryTime=" + recoveryTime +
+            " remaining=" + remaining +
+            " legacyIncapWeaken=" +
+                buff.hasBuff(player, "incapWeaken");
+    }
+}
