diff --git a/sku.0/sys.server/compiled/game/script/ai/imperial_presence/harass.java b/sku.0/sys.server/compiled/game/script/ai/imperial_presence/harass.java
--- a/sku.0/sys.server/compiled/game/script/ai/imperial_presence/harass.java
+++ b/sku.0/sys.server/compiled/game/script/ai/imperial_presence/harass.java
@@ -577,3 +577,3 @@
         int interests = utils.getIntScriptVar(self, gcw.SCRIPTVAR_SCAN_INTEREST);
-        int playerLevel = getLevel(target);
+        int playerLevel = script.library.skill.getPrecuEncounterDifficulty(target);
         if (playerLevel >= 75 && isEnemyJedi(self, target) && rand(1, 5) == 1)
diff --git a/sku.0/sys.server/compiled/game/script/city/imperial_crackdown/imperial_trouble.java b/sku.0/sys.server/compiled/game/script/city/imperial_crackdown/imperial_trouble.java
--- a/sku.0/sys.server/compiled/game/script/city/imperial_crackdown/imperial_trouble.java
+++ b/sku.0/sys.server/compiled/game/script/city/imperial_crackdown/imperial_trouble.java
@@ -221,3 +221,3 @@
                         if (!checkForPercentage(player)) {
-                            if (getLevel(player) > 10) {
+                            if (script.library.skill.getPrecuEncounterDifficulty(player) > 10) {
                                 chat.chat(self, new string_id(CONVO, "attacking_" + getFactionName(self)));
diff --git a/sku.0/sys.server/compiled/game/script/event/ewok_festival/loveday_reward_crossbow.java b/sku.0/sys.server/compiled/game/script/event/ewok_festival/loveday_reward_crossbow.java
--- a/sku.0/sys.server/compiled/game/script/event/ewok_festival/loveday_reward_crossbow.java
+++ b/sku.0/sys.server/compiled/game/script/event/ewok_festival/loveday_reward_crossbow.java
@@ -102,3 +102,3 @@
             obj_id[] thoseWhoHateMe = getHateList(player);
-            int playerLevel = getLevel(player);
+            int playerLevel = script.library.skill.getPrecuEncounterDifficulty(player);
             if (thoseWhoHateMe.length != 0)
diff --git a/sku.0/sys.server/compiled/game/script/event/halloween/song_book.java b/sku.0/sys.server/compiled/game/script/event/halloween/song_book.java
--- a/sku.0/sys.server/compiled/game/script/event/halloween/song_book.java
+++ b/sku.0/sys.server/compiled/game/script/event/halloween/song_book.java
@@ -40,3 +40,3 @@
                 {
-                    if (getLevel(player) > 81)
+                    if (script.library.skill.getPrecuEntertainerContentDifficulty(player) > 81)
                     {
diff --git a/sku.0/sys.server/compiled/game/script/event/lost_squadron/stolen_fighter.java b/sku.0/sys.server/compiled/game/script/event/lost_squadron/stolen_fighter.java
--- a/sku.0/sys.server/compiled/game/script/event/lost_squadron/stolen_fighter.java
+++ b/sku.0/sys.server/compiled/game/script/event/lost_squadron/stolen_fighter.java
@@ -212,3 +212,3 @@
         utils.setScriptVar(self, "already_spawned", 1);
-        int playerLevel = getLevel(whoTriggeredMe);
+        int playerLevel = script.library.skill.getPrecuEncounterDifficulty(whoTriggeredMe);
         int numThugs = rand(1, 3);
diff --git a/sku.0/sys.server/compiled/game/script/library/collection.java b/sku.0/sys.server/compiled/game/script/library/collection.java
--- a/sku.0/sys.server/compiled/game/script/library/collection.java
+++ b/sku.0/sys.server/compiled/game/script/library/collection.java
@@ -1340,3 +1340,3 @@
         }
-        if (getLevel(player) >= 10)
+        if (script.library.skill.getPrecuEncounterDifficulty(player) >= 10)
         {
diff --git a/sku.0/sys.server/compiled/game/script/library/groundquests.java b/sku.0/sys.server/compiled/game/script/library/groundquests.java
--- a/sku.0/sys.server/compiled/game/script/library/groundquests.java
+++ b/sku.0/sys.server/compiled/game/script/library/groundquests.java
@@ -1412,3 +1412,3 @@
     {
-        int level = getLevel(player);
+        int level = script.library.skill.getPrecuEncounterDifficulty(player);
         int xpCurrentLevel = dataTableGetInt("datatables/player/player_level.iff", level, "xp_required");
diff --git a/sku.0/sys.server/compiled/game/script/library/npe.java b/sku.0/sys.server/compiled/game/script/library/npe.java
--- a/sku.0/sys.server/compiled/game/script/library/npe.java
+++ b/sku.0/sys.server/compiled/game/script/library/npe.java
@@ -1257,3 +1257,3 @@
         {
-            if (getLevel(player) >= LEVEL_CAP)
+            if (script.library.skill.getPrecuEncounterDifficulty(player) >= LEVEL_CAP)
             {
diff --git a/sku.0/sys.server/compiled/game/script/library/performance.java b/sku.0/sys.server/compiled/game/script/library/performance.java
--- a/sku.0/sys.server/compiled/game/script/library/performance.java
+++ b/sku.0/sys.server/compiled/game/script/library/performance.java
@@ -2945,3 +2945,3 @@
             }
-            else if (getLevel(actor) < 82)
+            else if (script.library.skill.getPrecuEntertainerContentDifficulty(actor) < 82)
             {
diff --git a/sku.0/sys.server/compiled/game/script/library/smuggler.java b/sku.0/sys.server/compiled/game/script/library/smuggler.java
--- a/sku.0/sys.server/compiled/game/script/library/smuggler.java
+++ b/sku.0/sys.server/compiled/game/script/library/smuggler.java
@@ -183,3 +183,3 @@
             {
-                int playerLevel = getLevel(player);
+                int playerLevel = script.library.skill.getPrecuEncounterDifficulty(player);
                 cashMultiplier = FENCE_MULTIPLIER_LOW;
@@ -289,3 +289,3 @@
         {
-            int playerLevel = getLevel(player);
+            int playerLevel = script.library.skill.getPrecuEncounterDifficulty(player);
             cashMultiplier = FENCE_MULTIPLIER_LOW;
diff --git a/sku.0/sys.server/compiled/game/script/library/space_combat.java b/sku.0/sys.server/compiled/game/script/library/space_combat.java
--- a/sku.0/sys.server/compiled/game/script/library/space_combat.java
+++ b/sku.0/sys.server/compiled/game/script/library/space_combat.java
@@ -640,7 +640,7 @@
                                 if (!utils.isProfession(((obj_id) objPlayer), utils.TRADER) && !utils.isProfession(((obj_id) objPlayer), utils.ENTERTAINER)) {
-                                    int combatLevel = getLevel(((obj_id) objPlayer));
+                                    int combatLevel = script.library.skill.getPrecuEncounterDifficulty(((obj_id) objPlayer));
                                     if (combatLevel < 90) {
                                         float fltGroundXp = intAmount * GROUND_XP_PERCENT;
                                         int intGroundXp = (int) fltGroundXp;
-                                        xp.grant(((obj_id) objPlayer), "combat_general", intGroundXp);
+                                        xp.grantCombatStyleXp(((obj_id) objPlayer), xp.COMBAT_GENERAL, intGroundXp);
                                     }
diff --git a/sku.0/sys.server/compiled/game/script/library/township.java b/sku.0/sys.server/compiled/game/script/library/township.java
--- a/sku.0/sys.server/compiled/game/script/library/township.java
+++ b/sku.0/sys.server/compiled/game/script/library/township.java
@@ -44,3 +44,3 @@
         }
-        return getLevel(player) >= MIN_LEVEL || space_skill.isMasterPilot(player);
+        return script.library.skill.getPrecuEncounterDifficulty(player) >= MIN_LEVEL || space_skill.isMasterPilot(player);
     }
diff --git a/sku.0/sys.server/compiled/game/script/npc/static_quest/quest_convo.java b/sku.0/sys.server/compiled/game/script/npc/static_quest/quest_convo.java
--- a/sku.0/sys.server/compiled/game/script/npc/static_quest/quest_convo.java
+++ b/sku.0/sys.server/compiled/game/script/npc/static_quest/quest_convo.java
@@ -388,3 +388,3 @@
         {
-            int playerLevel = getLevel(speaker);
+            int playerLevel = script.library.skill.getPrecuEncounterDifficulty(speaker);
             int npcLevel = dataTableGetInt(datatable, questNum, "gating_level_amt");
